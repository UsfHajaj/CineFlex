@model RegisterVM
@{
    ViewData["Title"] = "Auth";
    Layout = "_SecLayout";
}


<!-- Auth Section -->
<div class="auth-center">
    <div class="hslhx-auth-container">
        <div class="hslhx-auth-image">
            <div class="hslhx-auth-image-content">
                <h2>أهلاً بك في CineFlex</h2>
                <p>المنصة الرائدة لمشاهدة الأفلام والمسلسلات بجودة عالية. انضم إلينا اليوم واستمتع بتجربة مشاهدة فريدة.</p>
            </div>
        </div>
        <div class="hslhx-auth-forms">
            <div class="hslhx-forms-container">
                <div class="hslhx-tabs">
                    <div class="hslhx-tab active" onclick="switchTab('login')">تسجيل الدخول</div>
                    <div class="hslhx-tab" onclick="switchTab('register')">حساب جديد</div>
                </div>

                @* <!-- Login Form --> *@
                @* <div id="login-form" class="hslhx-tab-content active"> *@
                @*         <form asp-action="Login" asp-controller="Account" method="post"> *@
                @*         <div class="hslhx-form-group"> *@
                @*             <label asp-for="Login.EmailAdress">البريد الإلكتروني</label> *@
                @*                 <input asp-for="Login.EmailAdress"  class="hslhx-form-control" placeholder="أدخل بريدك الإلكتروني"> *@
                @*                 <span asp-validation-for="Login.EmailAdress" class="text-danger"></span> *@
                @*         </div> *@
                @*         <div class="hslhx-form-group"> *@
                @*             <label asp-for="Login.Password">كلمة المرور</label> *@
                @*             <div class="hslhx-password-field"> *@
                @*                     <input asp-for="Login.Password"  class="hslhx-form-control" id="login-password" placeholder="أدخل كلمة المرور"> *@
                @*                 <button  type="button" class="hslhx-toggle-password" onclick="togglePassword('login-password')">👁️</button> *@
                @*                 <span asp-validation-for="Login.Password" class="text-danger"></span> *@
                @*             </div> *@
                @*         </div> *@
                @*         <div class="hslhx-form-check"> *@
                @*             <input type="checkbox" id="remember-me" class="hslhx-form-check-input"> *@
                @*             <label for="remember-me">تذكرني</label> *@
                @*         </div> *@
                @*         <a href="#" class="hslhx-forgot-password">نسيت كلمة المرور؟</a> *@
                @*         <button type="submit" class="hslhx-btn hslhx-btn-primary">تسجيل الدخول</button> *@
                @*     </form> *@

                @*     <div class="hslhx-social-login"> *@
                @*         <p>أو تسجيل الدخول عبر</p> *@
                @*         <div class="hslhx-social-buttons"> *@
                @*             <a href="#" class="hslhx-social-btn">G</a> *@
                @*             <a href="#" class="hslhx-social-btn">f</a> *@
                @*             <a href="#" class="hslhx-social-btn">t</a> *@
                @*         </div> *@
                @*     </div> *@

                @*     <p class="hslhx-form-note">ليس لديك حساب؟ <a href="#" onclick="switchTab('register')">إنشاء حساب جديد</a></p> *@
                @* </div> *@

                <!-- Register Form -->
                <div id="register-form" class="hslhx-tab-content">
                    <form asp-action="Register" asp-controller="Account" method="post">
                        <div class="hslhx-form-group">
                            <label asp-for="FullName">الاسم الكامل</label>
                            <input asp-for="FullName" class="hslhx-form-control">
                            <span asp-validation-for="FullName" class="text-danger"></span>
                        </div>
                        <div class="hslhx-form-group">
                            <label asp-for="EmailAdress">البريد الإلكتروني</label>
                            <input asp-for="EmailAdress" class="hslhx-form-control">
                            <span asp-validation-for="EmailAdress" class="text-danger"></span>
                        </div>
                        <div class="hslhx-form-group">
                            <label asp-for="Password">كلمة المرور</label>
                            <div class="hslhx-password-field">
                                <input asp-for="Password" id="register-password" class="hslhx-form-control">
                                <button type="button" class="hslhx-toggle-password" onclick="togglePassword('register-password')">👁️</button>
                                <span asp-validation-for="Password" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="hslhx-form-group">
                            <label asp-for="ConfirmPassword">تأكيد كلمة المرور</label>
                            <div class="hslhx-password-field">
                                <input asp-for="ConfirmPassword" id="register-confirm-password" class="hslhx-form-control">
                                <button type="button" class="hslhx-toggle-password" onclick="togglePassword('register-confirm-password')">👁️</button>
                                <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
                            </div>
                        </div>
                        <div class="hslhx-form-check">
                            <input type="checkbox" id="terms" class="hslhx-form-check-input">
                            <label for="terms">أوافق على <a href="#" style="color: var(--primary-color);">الشروط والأحكام</a></label>
                        </div>
                        <button type="submit" class="hslhx-btn hslhx-btn-primary">إنشاء حساب</button>
                    </form>

                    <div class="hslhx-social-login">
                        <p>أو التسجيل عبر</p>
                        <div class="hslhx-social-buttons">
                            <a href="#" class="hslhx-social-btn">G</a>
                            <a href="#" class="hslhx-social-btn">f</a>
                            <a href="#" class="hslhx-social-btn">t</a>
                        </div>
                    </div>

                    <p class="hslhx-form-note">لديك حساب بالفعل؟ <a href="#" onclick="switchTab('login')">تسجيل الدخول</a></p>
                </div>
            </div>
        </div>
    </div>
</div>
<script>
    function switchTab(tab) {
        // Hide all tab contents
        document.querySelectorAll('.hslhx-tab-content').forEach(content => {
            content.classList.remove('active');
        });

        // Remove active class from all tabs
        document.querySelectorAll('.hslhx-tab').forEach(tabElement => {
            tabElement.classList.remove('active');
        });

        // Show selected tab content
        document.getElementById(tab + '-form').classList.add('active');

        // Add active class to selected tab
        document.querySelector('.hslhx-tab:nth-child(' + (tab === 'login' ? '1' : '2') + ')').classList.add('active');
    }

    function togglePassword(inputId) {
        const passwordInput = document.getElementById(inputId);
        const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
        passwordInput.setAttribute('type', type);
    }
</script>